export interface TnavigationType {
  path: string;
  title: string;
  icon: string;
  role?: string[];
}

export type TCardPropsType = {
  icon: string;
  title: string;
  value: number | string;
  color: string;
};
